/**
 * Radar
 * Taylor Arnicar
 * 2-1-2011
 * Every ship will have a radar which it can query to get information about its surroundings.
 */

class Radar {
   public:
      virtual void Radar();
      virtual ~Radar();
      virtual getFullReading();
      virtual getNearbyReading();
      
      
   private:
   protected:

}
